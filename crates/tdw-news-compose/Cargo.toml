[package]
name = "tdw-news-compose"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT OR Apache-2.0"
description = "News-aggregation policy layer: round-robin, dedup, validate, truncate, cap."

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
