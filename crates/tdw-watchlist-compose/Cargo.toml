[package]
name = "tdw-watchlist-compose"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT OR Apache-2.0"
description = "Watchlist policy layer: validate, normalize, dedup by symbol, cap, stable order."

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
