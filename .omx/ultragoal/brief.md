Implement MCP daemon-backed tool execution end to end: add tdw-mcp daemon client configuration with localhost TCP default, env/config override, and fail-closed behavior when daemon-backed tools are requested while the daemon is unavailable; route a narrow first set of MCP tools through tdw-app-client and the daemon boundary, led by tdw.daemon.triage and a query/op submission style tool if the existing protocol supports it; keep deterministic fixture tools offline and fast; add focused unit tests plus an in-process daemon integration test without live Docker; update MCP boundary/readiness docs; preserve stdio and Streamable HTTP transports, clean-room constraints, repo worktree/PR workflow, and final verification/review gates.
